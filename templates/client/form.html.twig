{% extends 'base.html.twig' %} {% block main %}
<div class="container mx-auto p-6">
  <div class="bg-white shadow-md rounded-lg overflow-hidden">
    <div class="bg-blue-500 text-white text-lg font-bold px-4 py-2">
      Enregistrement d'un client
    </div>
    <div class="p-6">
      {{ form_start(formClient) }}
      {{ form_widget(formClient) }}
      <div class="flex justify-end mt-6">
        <button
          type="submit"
          class="ml-2 mb-4 px-4 py-2 border border-green-800 hover:bg-green-800 hover:text-white font-semibold rounded-md"
        >
          Save
        </button>
      </div>

      {{ form_end(formClient) }}
    </div>
  </div>
</div>

{% block javascripts %}
<script src="{{ asset('controllers/form_client.js') }}"></script>
{% endblock %} {% endblock %}
